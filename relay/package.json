{
  "name": "@iice257/codex-handoff-plus-relay",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "db:migrate:remote": "wrangler d1 migrations apply imessage-handoff --remote",
    "dev": "wrangler dev",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --test tests/*.test.ts",
    "deploy": "pnpm run db:migrate:remote && wrangler deploy",
    "types": "wrangler types"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^25.2.3",
    "typescript": "^5.9.3",
    "wrangler": "^4.67.0"
  }
}
